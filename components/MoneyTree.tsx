
// This component is obsolete in the points-based system.
// Keeping file to avoid breaking imports if any remain, but returning null.
import React from 'react';
export const MoneyTree: React.FC<any> = () => null;
